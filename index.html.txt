<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>For Judy ❤️</title>

<style>
body {
  text-align: center;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg, #ffb6c1, #ffe6f0);
  overflow: hidden;
  margin-top: 60px;
}

h1 {
  color: #ff0066;
  font-size: 30px;
}

button {
  padding: 12px 25px;
  font-size: 18px;
  margin: 10px;
  border-radius: 12px;
  border: none;
  cursor: pointer;
  transition: 0.3s;
}

.yes {
  background-color: #ff0066;
  color: white;
}

.no {
  background-color: gray;
  color: white;
  position: absolute;
}

.main-photo {
  width: 220px;
  border-radius: 20px;
  border: 4px solid white;
  box-shadow: 0 0 20px pink;
  animation: glow 2s infinite alternate;
}

@keyframes glow {
  from { box-shadow: 0 0 10px pink; }
  to { box-shadow: 0 0 25px red; }
}

.big-photo {
  width: 300px;
  border-radius: 25px;
  margin-top: 20px;
  animation: fadeIn 2s ease-in-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.8); }
  to { opacity: 1; transform: scale(1); }
}

.heart {
  position: absolute;
  animation: float 5s linear infinite;
}

@keyframes float {
  0% {transform: translateY(0);}
  100% {transform: translateY(-100vh);}
}
</style>
</head>

<body>

<img src="photo.jpg" class="main-photo">

<h1>Judy, will you be my Valentine? 💖</h1>

<button class="yes" onclick="yesClick()">Yes 💘</button>
<button class="no" id="noBtn" onmouseover="moveButton()">No 😜</button>

<script>
function yesClick() {
  document.body.innerHTML = `
    <h1>YAAAAY!!! 🎉💖</h1>
    <h2>You just made me the happiest person alive, Judy 😍</h2>
    <p>I promise to always choose you ❤️</p>
    <img src="photo.jpg" class="big-photo">
  `;
}

function moveButton() {
  const button = document.getElementById("noBtn");
  const x = Math.random() * (window.innerWidth - 100);
  const y = Math.random() * (window.innerHeight - 50);
  button.style.left = x + "px";
  button.style.top = y + "px";
}

setInterval(() => {
  const heart = document.createElement("div");
  heart.classList.add("heart");
  heart.innerHTML = "💖";
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = Math.random() * 20 + 15 + "px";
  document.body.appendChild(heart);

  setTimeout(() => {
    heart.remove();
  }, 5000);
}, 300);
</script>

</body>
</html>